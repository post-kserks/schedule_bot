# 📚 Telegram Bot: Расписание занятий

Умный Telegram бот для управления расписанием занятий с функциями уведомлений, контрольных мероприятий и удобным интерфейсом.

## 🚀 Быстрый старт

### Предварительные требования

- Python 3.11 или выше
- Telegram аккаунт
- Токен бота от [@BotFather](https://t.me/BotFather)

### Локальная установка

1. **Клонируйте репозиторий:**
```bash
git clone <your-repo-url>
cd schedule_bot
```

2. **Создайте виртуальное окружение:**
```bash
python -m venv venv
source venv/bin/activate  # Linux/MacOS
# или
venv\Scripts\activate     # Windows
```

3. **Установите зависимости:**
```bash
pip install -r requirements.txt
```

4. **Настройте конфигурацию:**
```bash
cp .env.example .env
```
Отредактируйте `.env` файл:
```env
BOT_TOKEN=your_actual_bot_token_here
ADMIN_USERNAME=your_telegram_username_without_@
```

5. **Запустите бота:**
```bash
python bot.py
```

### Запуск через Docker

1. **Соберите образ:**
```bash
docker build -t schedule-bot .
```

2. **Запустите контейнер:**
```bash
docker run -d \
  --name schedule-bot \
  --restart unless-stopped \
  --env-file .env \
  -v $(pwd)/data:/app/data \
  -v $(pwd)/logs:/app/logs \
  schedule-bot
```

Или используйте docker-compose:
```bash
docker-compose up -d
```

## 🤖 Взаимодействие с ботом

### Основные команды

- **`/start`** - Активация бота и показ клавиатуры
- **`/help`** - Справка по командам
- **`/today`** - Расписание на сегодня
- **`/tomorrow`** - Расписание на завтра  
- **`/week`** - Расписание на всю неделю

### Кнопки интерфейса

После команды `/start` появляется клавиатура с кнопками:

```
[📅 Сегодня] [📆 Завтра]
[📅 Неделя] [❓ Помощь]
```

Для администраторов дополнительная кнопка:
```
[⚙️ Админ-панель]
```

### Автоматические уведомления

- **📅 Ежедневно в 21:00** - автоматическая отправка расписания на завтра
- **🔔 За 10 минут до занятия** - напоминание о начале пары

## ⚙️ Административные функции

### Доступ к админ-панели

Только пользователь с username, указанным в `ADMIN_USERNAME`, имеет доступ к админ-панели через кнопку "⚙️ Админ-панель".

### Функции админ-панели

1. **📋 Список мероприятий** - просмотр всех контрольных мероприятий
2. **➕ Добавить мероприятие** - пошаговое добавление:
   - Ввод даты (формат: ГГГГ-ММ-ДД)
   - Ввод названия предмета
   - Ввод типа мероприятия
3. **❌ Удалить мероприятие** - удаление существующих мероприятий

### Пример контрольного мероприятия

```
Математический анализ [🚨 Контрольная работа]
```

## 📊 Структура расписания

### Формат данных

Расписание разделено на **числитель** и **знаменатель** с автоматическим переключением между неделями.

**Формат занятия:**
```
🕒 10:10-11:40
📚 Название предмета
🏫 Номер аудитории
📝 Тип занятия (лекция/семинар/лабораторная)
```

### Типы недель

Бот автоматически определяет тип недели на основе даты начала семестра (настраивается в `schedule.py`).

## 🗂️ Структура проекта

```
schedule_bot/
├── 📁 data/                 # Данные (база данных)
├── 📁 logs/                 # Логи приложения
├── 🐍 bot.py               # Основной файл бота
├── 🐍 config.py            # Конфигурация
├── 🐍 database.py          # Работа с базой данных
├── 🐍 schedule.py          # Логика расписания
├── 🐍 notifier.py          # Система уведомлений
├── 🐍 admin.py             # Админ-панель
├── 📄 requirements.txt     # Зависимости Python
├── 📄 .env                 # Переменные окружения
├── 📄 .env.example         # Пример конфигурации
├── 🐳 Dockerfile           # Конфигурация Docker
├── 📄 docker-compose.yml   # Docker Compose
└── 📄 README.md            # Документация
```

## 🔧 Конфигурация

### Переменные окружения (.env)

| Переменная | Описание | Пример |
|------------|-----------|---------|
| `BOT_TOKEN` | Токен бота от @BotFather | `123456:ABC-DEF1234ghIkl-zyx57W2v1u123ew11` |
| `ADMIN_USERNAME` | Telegram username администратора | `ivan_petrov` |

### Настройка расписания

Основное расписание настраивается в `schedule.py`:

```python
# Дата начала семестра для расчета недель
self.semester_start = datetime(2024, 9, 1)

# Логика числитель/знаменатель
def is_numerator_week(self, date: datetime = None) -> bool:
    # Настройте под ваше расписание
    return week_number % 2 == 0
```

## 🐳 Docker развертывание

### Production развертывание

1. **Сборка образа:**
```bash
docker build -t your-registry/schedule-bot:latest .
```

2. **Запуск:**
```bash
docker run -d \
  --name schedule-bot \
  --restart unless-stopped \
  -e BOT_TOKEN=your_token \
  -e ADMIN_USERNAME=your_username \
  -v schedule_bot_data:/app/data \
  -v schedule_bot_logs:/app/logs \
  your-registry/schedule-bot:latest
```

### Мониторинг

```bash
# Просмотр логов
docker logs schedule-bot

# Статистика контейнера
docker stats schedule-bot

# Перезапуск бота
docker restart schedule-bot
```

## 🛠️ Разработка

### Добавление новых предметов

Редактируйте методы `_parse_numerator_schedule()` и `_parse_denominator_schedule()` в `schedule.py`:

```python
Subject("Название предмета", "Аудитория", "Время начала", "Время окончания", "Тип занятия")
```

### Тестирование

```bash
# Запуск тестового скрипта
python test_bot.py

# Проверка работы БД
python -c "from database import db; print('БД работает')"
```

### Логирование

Логи хранятся в папке `logs/` и выводятся в консоль. Уровень логирования настраивается в `bot.py`.

## 📝 Формат данных

### База данных

Используется SQLite с двумя таблицами:

- **`control_events`** - контрольные мероприятия
- **`users`** - зарегистрированные пользователи

### Сообщения бота

Сообщения форматируются с использованием эмодзи для лучшей читаемости:

```
📅 Понедельник (Числитель)

🕒 10:10-11:40
📚 Математический анализ
🏫 301х
📝 лекция 🚨 Контрольная работа
```

## 🔄 Обновление расписания

Для обновления расписания на новый семестр:

1. Обновите дату `semester_start` в `schedule.py`
2. Отредактируйте расписание в методах `_parse_numerator_schedule()` и `_parse_denominator_schedule()`
3. Перезапустите бота

## 🐛 Поиск и решение проблем

### Частые проблемы

1. **Бот не отвечает на сообщения**
   - Проверьте токен бота в `.env`
   - Убедитесь, что бот не заблокирован

2. **Не приходят уведомления**
   - Проверьте настройки времени в `notifier.py`
   - Убедитесь, что JobQueue работает

3. **Ошибки базы данных**
   - Проверьте права доступа к папке `data/`
   - Убедитесь, что файл БД не заблокирован

### Логи

Логи содержат подробную информацию о работе бота. Используйте их для диагностики проблем.

## 📄 Лицензия

Этот проект распространяется под лицензией MIT.

## 🤝 Вклад в проект

Для внесения изменений:

1. Форкните репозиторий
2. Создайте feature ветку
3. Внесите изменения
4. Создайте Pull Request

## 📞 Поддержка

При возникновении проблем создавайте Issue в репозитории проекта.

---

**🎓 Успешного использования бота!**