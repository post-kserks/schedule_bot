# 📚 Telegram Bot: Расписание занятий

Умный Telegram бот для управления расписанием занятий с функциями уведомлений, контрольных мероприятий и удобным интерфейсом.

## 🚀 Быстрый старт

### Предварительные требования

- Python 3.11 или выше
- Telegram аккаунт
- Токен бота от [@BotFather](https://t.me/BotFather)
- Docker и Docker Compose (для контейнерного развертывания)

### Методы развертывания

Выберите один из способов развертывания:

1. **Локальный запуск** - для разработки и тестирования
2. **Docker контейнер** - для продакшн среды
3. **Docker Compose** - для упрощенного управления

## 🔧 Локальная установка (для разработки)

### 1. Клонируйте репозиторий
```bash
git clone https://github.com/post-kserks/schedule_bot.git
cd schedule_bot
```

### 2. Настройте виртуальное окружение
```bash
# Создание виртуального окружения
python -m venv venv

# Активация (Linux/MacOS)
source venv/bin/activate

# Активация (Windows)
venv\Scripts\activate
```

### 3. Установите зависимости
```bash
pip install -r requirements.txt
```

### 4. Настройте конфигурацию
```bash
# Копируем пример конфигурации
cp .env.example .env
```

Отредактируйте `.env` файл:
```env
BOT_TOKEN=your_actual_bot_token_here
ADMIN_USERNAME=your_telegram_username_without_@
```

**Как получить BOT_TOKEN:**
1. Напишите [@BotFather](https://t.me/BotFather) в Telegram
2. Используйте команду `/newbot`
3. Следуйте инструкциям и получите токен
4. Вставьте токен в `.env` файл

### 5. Запустите бота
```bash
python bot.py
```

## 🐳 Развертывание с помощью Docker

### Быстрый запуск с build-and-run.sh

Самый простой способ развернуть бота:

```bash
# Делаем скрипт исполняемым
chmod +x build-and-run.sh

# Запускаем скрипт
./build-and-run.sh
```

**Что делает скрипт:**
- Проверяет наличие `.env` файла
- Создает его из примера если нужно
- Собирает Docker образ
- Запускает контейнер с правильными настройками
- Показывает команды для просмотра логов

### Ручная настройка Docker

Если хотите настроить вручную:

#### 1. Создайте .env файл
```bash
cp .env.example .env
# Отредактируйте .env - добавьте BOT_TOKEN и ADMIN_USERNAME
```

#### 2. Соберите Docker образ
```bash
docker build -t schedule-bot .
```

#### 3. Запустите контейнер
```bash
docker run -d \
  --name schedule-bot \
  --restart unless-stopped \
  --env-file .env \
  -v $(pwd)/data:/app/data \
  -v $(pwd)/logs:/app/logs \
  schedule-bot
```

### Управление Docker контейнером

#### Просмотр состояния
```bash
# Просмотр запущенных контейнеров
docker ps

# Просмотр всех контейнеров (включая остановленные)
docker ps -a
```

#### Просмотр логов
```bash
# Просмотр логов в реальном времени
docker logs -f schedule-bot

# Просмотр последних 100 строк логов
docker logs --tail 100 schedule-bot
```

#### Остановка контейнера
```bash
# Остановка контейнера
docker stop schedule-bot

# Принудительная остановка (если не реагирует)
docker kill schedule-bot
```

#### Перезапуск контейнера
```bash
# Перезапуск контейнера
docker restart schedule-bot
```

#### Удаление контейнера
```bash
# Остановка и удаление контейнера
docker stop schedule-bot && docker rm schedule-bot

# Принудительное удаление (без остановки)
docker rm -f schedule-bot
```

#### Обновление контейнера
```bash
# Остановить текущий контейнер
docker stop schedule-bot

# Удалить контейнер
docker rm schedule-bot

# Получить последние изменения из git
git pull

# Пересобрать образ
docker build -t schedule-bot .

# Запустить новый контейнер
docker run -d \
  --name schedule-bot \
  --restart unless-stopped \
  --env-file .env \
  -v $(pwd)/data:/app/data \
  -v $(pwd)/logs:/app/logs \
  schedule-bot
```

## 🎯 Развертывание с Docker Compose

### Запуск
```bash
# Запуск в фоновом режиме
docker-compose up -d

# Запуск с просмотром логов
docker-compose up
```

### Управление с Docker Compose

```bash
# Просмотр логов
docker-compose logs -f

# Остановка
docker-compose down

# Перезапуск
docker-compose restart

# Просмотр статуса
docker-compose ps
```

### Остановка и очистка
```bash
# Остановка и удаление контейнеров
docker-compose down

# Остановка с удалением volumes (полная очистка)
docker-compose down -v
```

## 🤖 Взаимодействие с ботом

### Первый запуск

1. Найдите бота в Telegram по имени
2. Отправьте команду `/start`
3. Бот активируется и покажет клавиатуру с кнопками

### Основные команды

- **`/start`** - Активация бота и показ клавиатуры
- **`/help`** - Справка по командам
- **`/today`** - Расписание на сегодня
- **`/tomorrow`** - Расписание на завтра  
- **`/week`** - Расписание на всю неделю

### Кнопки интерфейса

После команды `/start` появляется клавиатура:

**Для обычных пользователей:**
```
[📅 Сегодня] [📆 Завтра]
[📅 Неделя] [❓ Помощь]
```

**Для администраторов:**
```
[📅 Сегодня] [📆 Завтра]
[📅 Неделя] [❓ Помощь]
[⚙️ Админ-панель]
```

### Автоматические уведомления

- **📅 Ежедневно в 21:00** - автоматическая отправка расписания на завтра
- **🔔 За 10 минут до занятия** - напоминание о начале пары

## ⚙️ Административные функции

### Настройка администратора

В файле `.env` укажите ваш Telegram username (без @):
```env
ADMIN_USERNAME=your_username
```

### Функции админ-панели

1. **📋 Список мероприятий** - просмотр всех контрольных мероприятий
2. **➕ Добавить мероприятие** - пошаговое добавление мероприятия
3. **❌ Удалить мероприятие** - удаление существующих мероприятий

### Процесс добавления мероприятия

1. Нажмите "⚙️ Админ-панель"
2. Выберите "➕ Добавить мероприятие"
3. Введите дату в формате `ГГГГ-ММ-ДД` (например, `2024-01-15`)
4. Введите название предмета
5. Введите тип мероприятия (например, "контрольная работа")

## 🗂️ Структура проекта

```
schedule_bot/
├── 📁 data/                 # Данные (база данных) - создается автоматически
├── 📁 logs/                 # Логи приложения - создается автоматически
├── 🐍 bot.py               # Основной файл бота
├── 🐍 config.py            # Конфигурация приложения
├── 🐍 database.py          # Работа с базой данных
├── 🐍 schedule.py          # Логика расписания
├── 🐍 notifier.py          # Система уведомлений
├── 🐍 admin.py             # Админ-панель
├── 📄 requirements.txt     # Зависимости Python
├── 📄 .env                 # Переменные окружения (создается вами)
├── 📄 .env.example         # Пример конфигурации
├── 🐳 Dockerfile           # Конфигурация Docker
├── 📄 docker-compose.yml   # Docker Compose конфигурация
├── 📄 build-and-run.sh     # Скрипт быстрого развертывания
└── 📄 README.md            # Документация
```

## 🔧 Конфигурация

### Переменные окружения

Создайте файл `.env` в корне проекта:

```env
# Токен бота от @BotFather
BOT_TOKEN=123456:ABC-DEF1234ghIkl-zyx57W2v1u123ew11

# Telegram username администратора (без @)
ADMIN_USERNAME=your_username
```

### Настройка расписания

Основное расписание настраивается в `schedule.py`:

```python
# Дата начала семестра для расчета недель
self.semester_start = datetime(2024, 9, 1)

# Логика переключения числитель/знаменатель
def is_numerator_week(self, date: datetime = None) -> bool:
    # Настройте под ваше расписание
    return week_number % 2 == 0
```

## 🐳 Production развертывание

### Рекомендации для продакшн

1. **Используйте Docker** для изоляции и стабильности
2. **Настройте автоматические бэкапы** папки `data/`
3. **Настройте мониторинг** логов в папке `logs/`
4. **Используйте reverse proxy** если нужно (nginx, traefik)

### Команды для мониторинга

```bash
# Просмотр использования ресурсов
docker stats schedule-bot

# Проверка здоровья контейнера
docker exec schedule-bot python -c "from database import db; print('OK')"

# Просмотр размера логов
du -sh logs/
```

## 🛠️ Разработка и обновление

### Добавление новых предметов

Редактируйте методы в `schedule.py`:

```python
# В _parse_numerator_schedule() и _parse_denominator_schedule()
Subject("Название предмета", "Аудитория", "Время начала", "Время окончания", "Тип занятия")
```

### Обновление бота при изменениях

```bash
# Остановить текущую версию
docker stop schedule-bot

# Получить обновления
git pull

# Пересобрать и запустить
docker build -t schedule-bot .
docker run -d [параметры как выше]
```

Или используйте скрипт:
```bash
./build-and-run.sh
```

## 🐛 Поиск и решение проблем

### Частые проблемы и решения

**Бот не отвечает на сообщения:**
- Проверьте токен бота в `.env`
- Убедитесь, что бот не заблокирован
- Проверьте логи: `docker logs schedule-bot`

**Не приходят уведомления:**
- Проверьте настройки времени в системе
- Убедитесь, что контейнер работает: `docker ps`
- Проверьте логи уведомлений

**Ошибки базы данных:**
- Проверьте права доступа к папке `data/`
- Убедитесь, что файл БД не заблокирован
- Проверьте логи БД

### Получение помощи

1. **Проверьте логи:** `docker logs schedule-bot`
2. **Проверьте конфигурацию:** убедитесь в правильности `.env`
3. **Создайте Issue** на GitHub с описанием проблемы и логами

## 📄 Лицензия

Этот проект распространяется под лицензией MIT.

## 🤝 Вклад в проект

Мы приветствуем вклад в проект! 

1. Форкните репозиторий
2. Создайте feature ветку
3. Внесите изменения
4. Создайте Pull Request

## 📞 Поддержка

При возникновении проблем:
1. Проверьте этот README
2. Изучите логи контейнера
3. Создайте Issue на GitHub

---

**🎓 Успешного использования бота!**

*Последнее обновление: Октябрь 2024*